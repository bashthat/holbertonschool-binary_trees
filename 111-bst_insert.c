#include "binary_trees.h"

/**
 * bst_insert - insert a node in a binary search tree
 * @tree:  is a double pointer to the root node and the BST insert value
 * @value: is the value to store and inserted into the tree node
 * Return: pointer of node, or NULL on failure
 */

bst_t *bst_insert(bst_t **tree, int value)

{
    
    return (NULL);



}